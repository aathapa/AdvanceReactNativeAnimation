import React, { Component } from 'react';
import { View, Text, } from 'react-native';
import { ColorInterPolation } from '../../components/';

export default class Interpolation extends Component {
  render() {
    return (
      <View>
        <ColorInterPolation />
      </View>
    );
  }
}
